{
  "name": "deckbuilder-server",
  "version": "1.0.0",
  "description": "server for duelyst.cards",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "echo starting DEV MODE server -- not for production \\(press Ctrl-C to end\\) && babel-watch src/app.js",
    "build": "rm -rf prod/* && babel src --out-dir prod --source-maps",
    "test": "npm run build && mocha",
    "testonly": "mocha"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-core": "^6.18.2",
    "babel-plugin-transform-async-to-generator": "^6.16.0",
    "babel-plugin-transform-exponentiation-operator": "^6.8.0",
    "babel-preset-node6": "^11.0.0",
    "babel-watch": "^2.0.3",
    "eslint": "^3.9.1",
    "mocha": "^3.1.2"
  },
  "dependencies": {
    "babel-runtime": "^6.18.0",
    "config": "^1.24.0",
    "kcors": "^1.3.1",
    "koa": "^2.0.0",
    "koa-better-body": "^3.0.2",
    "koa-bodyparser": "^2.3.0",
    "koa-router": "next",
    "mongoose": "^4.6.8"
  }
}
